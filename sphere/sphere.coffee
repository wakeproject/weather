###
  sphere.coffee
###
define [
  'exports'
  'cs!fields'
], (m, f) ->

    m.field = (name, kind) ->
        #TODO
        abstract
        concrete
        proxy
        proxy

    m.bind = (variable, binding, dependencies) ->
        #TODO

    m.init = (variable, binding, dependencies) ->
        #TODO

    m
